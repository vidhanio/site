default_job = "run"

[jobs.run]
background = false
command = ["cargo", "run"]
need_stdout = true
on_change_strategy = "kill_then_restart"
watch = ["assets", "build", "typst", "resume", "src", ".env"]
